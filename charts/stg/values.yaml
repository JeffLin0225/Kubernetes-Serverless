global:
  namespace: ns-sep-stg

quotas:
  systems:
    crawler:
      cpu_request: "100m"
      memory_request: "128Mi"
      cpu_limit: "250m"
      memory_limit: "256Mi"

engine:
  replicas: 1
  port: 8080
  image:
    repository: sep-engine
    tag: stg
    pullPolicy: Never
