一．

兩個 WARNING
1. Debug mode

Running in "debug" mode. Switch to "release" mode in production.
開發階段沒問題，上 K8s 前記得加：

go
gin.SetMode(gin.ReleaseMode)
或設環境變數 GIN_MODE=release。

二．

Trusted Proxies（比較要注意）

You trusted all proxies, this is NOT safe.
Gin 預設信任所有代理，意思是任何人都可以偽造 X-Forwarded-For header 讓你以為請求來自別的 IP。上 K8s 後你前面會有 ingress / load balancer，建議在 router 加一行：

go
r := gin.Default()
r.SetTrustedProxies(nil)  // 不信任任何 proxy（直連模式）
// 或明確指定你的 ingress IP
// r.SetTrustedProxies([]string{"10.0.0.0/8"})

3. 

cron 探針服務就好

4. 

增加告警與異常死 Pod 自動清理機制（Cleaner / Watcher）
- 問題：當 Pod 發生 `ErrImageNeverPull`、`ImagePullBackOff` 等啟動失敗時，Pod 會卡在等待重試，持續霸佔 K8s Node 的 CPU/Memory 預約配額（Requests）。但若寫死 ActiveDeadlineSeconds 又會誤殺跑很久的正常任務。
- 解法：透過獨立 Cleaner 服務或背景 Watcher Goroutine 監聽 Pod 狀態：
  - 若狀態為 `Running`：不干預，讓任務正常跑完。
  - 若狀態為 `ErrImageNeverPull` / `ImagePullBackOff` 等不可逆啟動失敗：立即刪除該 Job 釋放配額並發出告警。
- 手動清理指令：
  ```bash
  # 刪除特定 Job（會連帶刪除底下 Pod）
  kubectl delete job <job-name>

  # 一鍵清理所有由 Serverless Engine 管理的 Job
  kubectl delete jobs -l app.kubernetes.io/managed-by=serverless-engine

  # 刪除特定卡住的 Pod
  kubectl delete pod <pod-name> --force --grace-period=0
  ```

5. 

ConfigMap 配額 In-Memory 快取 / Informer 機制
- 問題：目前在 resolveSystemResources 中，每次收到 /api/run 請求都會發出 Get ConfigMap 呼叫，高並發時會對 kube-apiserver 造成壓力與額外延遲。
- 改善：引入 In-Memory 快取（TTL Cache）或利用 client-go Informer / Watcher 監聽 ConfigMap 異動事件，即時更新本地配額快取。

6.

補強健康檢查機制（Health Check + K8s 原生探針）
- 問題：`services/engine/controller/health.go` 目前固定回傳 `{"status":"OK"}`，不檢查任何內部狀態（例如與 K8s apiserver 的連線、ConfigMap 讀取是否正常），engine 內部真的出問題時 `/health` 仍會回 200。另外 `charts/` 目前完全沒有設定 `livenessProbe` / `readinessProbe`。
- 解法：
  - 在 `health.go` 補上真實健康判斷（至少檢查 K8s client 是否能連上 apiserver）。
  - 在 Helm chart 的 Deployment template 補上 `livenessProbe`（存活探測，失敗重啟容器）與 `readinessProbe`（就緒探測，失敗只從 Service endpoints 移除、不重啟），交給 K8s 原生機制處理，不要自己另外做一個 cron 探針服務打自己（服務自己掛了會沒辦法回應自己的健康檢查，這是反模式）。
  - 若需要「服務掛了要告警通知」，應接 Prometheus/Alertmanager 等外部系統監控 Pod restart 次數，而非服務內部自建 loop。